// Code generated by sqlc. DO NOT EDIT.
// versions:
//   sqlc v1.31.1
// source: admin.sql

package db

import (
	"context"

	"github.com/jackc/pgx/v5/pgtype"
)

const getDynamicApplicationsSummary = `-- name: GetDynamicApplicationsSummary :many
WITH tenant AS (
    SELECT id
    FROM tenants
    WHERE tenant_uuid = $1
    LIMIT 1
)
SELECT
    a.id,
    a.client_id,
    a.application_name,
    a.is_enabled,
    a.created_at,
    a.updated_at,
    a.last_used_at,
    p.id AS profile_id,
    p.profile_name,
    g.id AS group_id,
    g.group_name
FROM applications a
JOIN tenant ON a.tenant_id = tenant.id
JOIN application_profiles p ON a.profile_id = p.id AND a.tenant_id = p.tenant_id
JOIN application_groups g ON a.group_id = g.id AND a.tenant_id = g.tenant_id
WHERE a.is_dynamic = TRUE
ORDER BY a.last_used_at DESC
`

type GetDynamicApplicationsSummaryRow struct {
	ID              pgtype.UUID        `json:"id"`
	ClientID        string             `json:"client_id"`
	ApplicationName string             `json:"application_name"`
	IsEnabled       bool               `json:"is_enabled"`
	CreatedAt       pgtype.Timestamptz `json:"created_at"`
	UpdatedAt       pgtype.Timestamptz `json:"updated_at"`
	LastUsedAt      pgtype.Timestamptz `json:"last_used_at"`
	ProfileID       pgtype.UUID        `json:"profile_id"`
	ProfileName     string             `json:"profile_name"`
	GroupID         pgtype.UUID        `json:"group_id"`
	GroupName       string             `json:"group_name"`
}

// Feeds your high-volume Fleet Monitoring view page
func (q *Queries) GetDynamicApplicationsSummary(ctx context.Context, tenantUuid pgtype.UUID) ([]GetDynamicApplicationsSummaryRow, error) {
	rows, err := q.db.Query(ctx, getDynamicApplicationsSummary, tenantUuid)
	if err != nil {
		return nil, err
	}
	defer rows.Close()
	items := []GetDynamicApplicationsSummaryRow{}
	for rows.Next() {
		var i GetDynamicApplicationsSummaryRow
		if err := rows.Scan(
			&i.ID,
			&i.ClientID,
			&i.ApplicationName,
			&i.IsEnabled,
			&i.CreatedAt,
			&i.UpdatedAt,
			&i.LastUsedAt,
			&i.ProfileID,
			&i.ProfileName,
			&i.GroupID,
			&i.GroupName,
		); err != nil {
			return nil, err
		}
		items = append(items, i)
	}
	if err := rows.Err(); err != nil {
		return nil, err
	}
	return items, nil
}

const getStaticApplicationsSummary = `-- name: GetStaticApplicationsSummary :many
WITH tenant AS (
    SELECT id
    FROM tenants
    WHERE tenant_uuid = $1
    LIMIT 1
)
SELECT
    a.id,
    a.client_id,
    a.application_name,
    a.is_enabled,
    a.created_at,
    a.updated_at,
    a.last_used_at,
    p.id AS profile_id,
    p.profile_name,
    g.id AS group_id,
    g.group_name
FROM applications a
JOIN tenant ON a.tenant_id = tenant.id
JOIN application_profiles p ON a.profile_id = p.id AND a.tenant_id = p.tenant_id
JOIN application_groups g ON a.group_id = g.id AND a.tenant_id = g.tenant_id
WHERE a.is_dynamic = FALSE
ORDER BY a.application_name ASC
`

type GetStaticApplicationsSummaryRow struct {
	ID              pgtype.UUID        `json:"id"`
	ClientID        string             `json:"client_id"`
	ApplicationName string             `json:"application_name"`
	IsEnabled       bool               `json:"is_enabled"`
	CreatedAt       pgtype.Timestamptz `json:"created_at"`
	UpdatedAt       pgtype.Timestamptz `json:"updated_at"`
	LastUsedAt      pgtype.Timestamptz `json:"last_used_at"`
	ProfileID       pgtype.UUID        `json:"profile_id"`
	ProfileName     string             `json:"profile_name"`
	GroupID         pgtype.UUID        `json:"group_id"`
	GroupName       string             `json:"group_name"`
}

// Feeds your Administrative Integration view page
func (q *Queries) GetStaticApplicationsSummary(ctx context.Context, tenantUuid pgtype.UUID) ([]GetStaticApplicationsSummaryRow, error) {
	rows, err := q.db.Query(ctx, getStaticApplicationsSummary, tenantUuid)
	if err != nil {
		return nil, err
	}
	defer rows.Close()
	items := []GetStaticApplicationsSummaryRow{}
	for rows.Next() {
		var i GetStaticApplicationsSummaryRow
		if err := rows.Scan(
			&i.ID,
			&i.ClientID,
			&i.ApplicationName,
			&i.IsEnabled,
			&i.CreatedAt,
			&i.UpdatedAt,
			&i.LastUsedAt,
			&i.ProfileID,
			&i.ProfileName,
			&i.GroupID,
			&i.GroupName,
		); err != nil {
			return nil, err
		}
		items = append(items, i)
	}
	if err := rows.Err(); err != nil {
		return nil, err
	}
	return items, nil
}
