// Code generated by sqlc. DO NOT EDIT.
// versions:
//   sqlc v1.31.1
// source: interaction_sessions.sql

package db

import (
	"context"

	"github.com/jackc/pgx/v5/pgtype"
)

const consumeInteractionSession = `-- name: ConsumeInteractionSession :one
WITH deleted AS (
    DELETE FROM interaction_sessions
    WHERE id = $1::uuid
      AND tenant_id = (SELECT id FROM tenants WHERE tenant_uuid = $2::uuid LIMIT 1) -- Enforces named consistency
    RETURNING
        id,
        client_id,
        redirect_uri,
        code_challenge,
        code_challenge_method,
        idp_hint,
        expires_at,
        state,
        nonce,
        acr_values,
        (SELECT tenant_uuid FROM tenants WHERE id = tenant_id) AS tenant_uuid
)
SELECT
    id,
    client_id,
    redirect_uri,
    code_challenge,
    code_challenge_method,
    idp_hint,
    expires_at,
    state,
    nonce,
    acr_values,
    tenant_uuid
FROM deleted
LIMIT 1
`

type ConsumeInteractionSessionParams struct {
	ID         pgtype.UUID `json:"id"`
	TenantUuid pgtype.UUID `json:"tenant_uuid"`
}

type ConsumeInteractionSessionRow struct {
	ID                  pgtype.UUID        `json:"id"`
	ClientID            string             `json:"client_id"`
	RedirectUri         string             `json:"redirect_uri"`
	CodeChallenge       string             `json:"code_challenge"`
	CodeChallengeMethod string             `json:"code_challenge_method"`
	IdpHint             *string            `json:"idp_hint"`
	ExpiresAt           pgtype.Timestamptz `json:"expires_at"`
	State               string             `json:"state"`
	Nonce               string             `json:"nonce"`
	AcrValues           string             `json:"acr_values"`
	TenantUuid          pgtype.UUID        `json:"tenant_uuid"`
}

func (q *Queries) ConsumeInteractionSession(ctx context.Context, arg ConsumeInteractionSessionParams) (ConsumeInteractionSessionRow, error) {
	row := q.db.QueryRow(ctx, consumeInteractionSession, arg.ID, arg.TenantUuid)
	var i ConsumeInteractionSessionRow
	err := row.Scan(
		&i.ID,
		&i.ClientID,
		&i.RedirectUri,
		&i.CodeChallenge,
		&i.CodeChallengeMethod,
		&i.IdpHint,
		&i.ExpiresAt,
		&i.State,
		&i.Nonce,
		&i.AcrValues,
		&i.TenantUuid,
	)
	return i, err
}

const getInteractionSession = `-- name: GetInteractionSession :one
SELECT
    id,
    client_id,
    redirect_uri,
    code_challenge,
    code_challenge_method,
    idp_hint,
    expires_at,
    state,
    nonce,
    acr_values,
    (SELECT tenant_uuid FROM tenants WHERE id = tenant_id) AS tenant_uuid
FROM interaction_sessions
WHERE id = $1::uuid
  AND tenant_id = (SELECT id FROM tenants WHERE tenant_uuid = $2::uuid LIMIT 1)
`

type GetInteractionSessionParams struct {
	ID         pgtype.UUID `json:"id"`
	TenantUuid pgtype.UUID `json:"tenant_uuid"`
}

type GetInteractionSessionRow struct {
	ID                  pgtype.UUID        `json:"id"`
	ClientID            string             `json:"client_id"`
	RedirectUri         string             `json:"redirect_uri"`
	CodeChallenge       string             `json:"code_challenge"`
	CodeChallengeMethod string             `json:"code_challenge_method"`
	IdpHint             *string            `json:"idp_hint"`
	ExpiresAt           pgtype.Timestamptz `json:"expires_at"`
	State               string             `json:"state"`
	Nonce               string             `json:"nonce"`
	AcrValues           string             `json:"acr_values"`
	TenantUuid          pgtype.UUID        `json:"tenant_uuid"`
}

func (q *Queries) GetInteractionSession(ctx context.Context, arg GetInteractionSessionParams) (GetInteractionSessionRow, error) {
	row := q.db.QueryRow(ctx, getInteractionSession, arg.ID, arg.TenantUuid)
	var i GetInteractionSessionRow
	err := row.Scan(
		&i.ID,
		&i.ClientID,
		&i.RedirectUri,
		&i.CodeChallenge,
		&i.CodeChallengeMethod,
		&i.IdpHint,
		&i.ExpiresAt,
		&i.State,
		&i.Nonce,
		&i.AcrValues,
		&i.TenantUuid,
	)
	return i, err
}

const saveInteractionSession = `-- name: SaveInteractionSession :exec
WITH tenant AS (
    SELECT id
    FROM tenants
    WHERE tenant_uuid = $11::uuid -- Safe named configuration parameter
    LIMIT 1
)
INSERT INTO interaction_sessions (
    id,
    tenant_id,
    client_id,
    redirect_uri,
    code_challenge,
    code_challenge_method,
    idp_hint,
    expires_at,
    state,
    nonce,
    acr_values
)
SELECT
    $1::uuid,
    tenant.id,
    $2,
    $3,
    $4,
    $5,
    $6,
    $7::timestamptz,
    $8,
    $9,
    $10
FROM tenant
`

type SaveInteractionSessionParams struct {
	ID                  pgtype.UUID        `json:"id"`
	ClientID            string             `json:"client_id"`
	RedirectUri         string             `json:"redirect_uri"`
	CodeChallenge       string             `json:"code_challenge"`
	CodeChallengeMethod string             `json:"code_challenge_method"`
	IdpHint             *string            `json:"idp_hint"`
	ExpiresAt           pgtype.Timestamptz `json:"expires_at"`
	State               string             `json:"state"`
	Nonce               string             `json:"nonce"`
	AcrValues           string             `json:"acr_values"`
	TenantUuid          pgtype.UUID        `json:"tenant_uuid"`
}

func (q *Queries) SaveInteractionSession(ctx context.Context, arg SaveInteractionSessionParams) error {
	_, err := q.db.Exec(ctx, saveInteractionSession,
		arg.ID,
		arg.ClientID,
		arg.RedirectUri,
		arg.CodeChallenge,
		arg.CodeChallengeMethod,
		arg.IdpHint,
		arg.ExpiresAt,
		arg.State,
		arg.Nonce,
		arg.AcrValues,
		arg.TenantUuid,
	)
	return err
}
