// Code generated by sqlc. DO NOT EDIT.
// versions:
//   sqlc v1.31.1
// source: housekeeping.sql

package db

import (
	"context"
)

const pruneExpiredAuthSessions = `-- name: PruneExpiredAuthSessions :exec
DELETE FROM auth_sessions WHERE expires_at <= NOW()
`

// PruneExpiredAuthSessions evicts old backplane authorization code session keys
// that have naturally expired without being consumed.
func (q *Queries) PruneExpiredAuthSessions(ctx context.Context) error {
	_, err := q.db.Exec(ctx, pruneExpiredAuthSessions)
	return err
}

const pruneExpiredDPoPProofs = `-- name: PruneExpiredDPoPProofs :exec
DELETE FROM dpop_proofs WHERE expires_at <= NOW()
`

// PruneExpiredDPoPProofs drops old device-binding proof thumbprints
// whose replay protection time windows have closed.
func (q *Queries) PruneExpiredDPoPProofs(ctx context.Context) error {
	_, err := q.db.Exec(ctx, pruneExpiredDPoPProofs)
	return err
}

const pruneExpiredInteractionSessions = `-- name: PruneExpiredInteractionSessions :exec
DELETE FROM interaction_sessions WHERE expires_at <= NOW()
`

// PruneExpiredInteractionSessions clears out stale login and authorization panel
// interaction wizard states after their tracking deadline passes.
func (q *Queries) PruneExpiredInteractionSessions(ctx context.Context) error {
	_, err := q.db.Exec(ctx, pruneExpiredInteractionSessions)
	return err
}

const pruneExpiredPushedAuthRequests = `-- name: PruneExpiredPushedAuthRequests :exec
DELETE FROM pushed_authorization_requests WHERE expires_at <= NOW()
`

// PruneExpiredPushedAuthRequests wipes out stale PAR endpoints from memory
// that were initiated but never exchanged for an authorization path.
func (q *Queries) PruneExpiredPushedAuthRequests(ctx context.Context) error {
	_, err := q.db.Exec(ctx, pruneExpiredPushedAuthRequests)
	return err
}

const pruneExpiredRevokedTokens = `-- name: PruneExpiredRevokedTokens :exec
DELETE FROM revoked_tokens WHERE expires_at <= NOW()
`

// PruneExpiredRevokedTokens purges records from the blacklisted token cache
// that have outlived their validation window threshold.
func (q *Queries) PruneExpiredRevokedTokens(ctx context.Context) error {
	_, err := q.db.Exec(ctx, pruneExpiredRevokedTokens)
	return err
}
